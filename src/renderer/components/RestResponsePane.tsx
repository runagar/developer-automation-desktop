import React from 'react';

/**
 * Placeholder body for the REST Response panel.
 * Functionality is implemented in feature R4.
 */
export default function RestResponsePane(): React.ReactElement {
  return (
    <div className="app-empty">
      <div className="app-empty__text">REST RESPONSE</div>
      <div className="app-empty__sub">
        FORMATTED RESPONSES FROM EXECUTED CALLS — NOT YET IMPLEMENTED
      </div>
    </div>
  );
}
