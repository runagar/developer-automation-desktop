import React from 'react';

/**
 * Placeholder body for the REST Crafter panel.
 * Functionality is implemented in feature R3.
 */
export default function RestCrafterPane(): React.ReactElement {
  return (
    <div className="app-empty">
      <div className="app-empty__text">REST CRAFTER</div>
      <div className="app-empty__sub">
        COMPOSE AND EXECUTE A REST CALL — NOT YET IMPLEMENTED
      </div>
    </div>
  );
}
